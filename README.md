# Flight Search MCP

This project scrapes flight data from various websites.

## Usage

### Expedia Scraper

To run the scraper for Expedia, execute the following command:

```bash
python scrapper/expedia/expedia_scrap.py
```

This will scrape the data from the website.

For data extraction from the scraped content using Beautiful Soup, run:

```bash
python scrapper/expedia/data_extraction.py
```

### MMT Scraper

To run the scraper for MMT, execute the following command:

```bash
python scrapper/mmt/mmt_scrap.py
```

This will scrape the data from the website.

For data extraction from the scraped content using Beautiful Soup, run:

```bash
python scrapper/mmt/data_extraction.py
```

### Ixigo Scraper

To run the scraper for Ixigo, execute the following command:

```bash
python scrapper/ixigo/ixigo_scrap.py
```

This will scrape the data from the website.

For data extraction from the scraped content using Beautiful Soup, run:

```bash
python scrapper/ixigo/data_extraction.py
```
